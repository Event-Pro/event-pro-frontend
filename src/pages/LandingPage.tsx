// Purpose - First page seen by users

// Functionality

// Search:
// - Drop down menu to select "Event Tag Name"
// - Redirect to CategoryEventPage based on event tag

// Hero Area
// - Category cards for each event tag
// - OnClick Redirect to CategoryEventPage based on event tag

// Lists out all events:
// - Show "EventCard" for each event created by mapping over all events in system. (Event cards display a portion of the event details)
// - OnClick Redirect to EventDetailPage based on event tag
import React from 'react'

function LandingPage() {
  return <div>LandingPage</div>
}

export default LandingPage
